-- ============================================================
-- קלוריות מדויקות מתווית — עמודת kcal_g בטבלאות התזונה
-- הרץ ב-Supabase SQL Editor
-- ============================================================
-- מטרה: כשמצלמים תווית ערכים תזונתיים על גב מוצר, קוראים את
--       מספר הקלוריות שהיצרן הדפיס בפועל (מדויק יותר מחישוב
--       מהמאקרו, בגלל ממתיקים/סיבים). שומרים אותו ל-100 גרם.
--       null = אין קלוריות מדויקות, ליפול לחישוב מהמאקרו כרגיל.
-- ============================================================

ALTER TABLE public.food_log
  ADD COLUMN IF NOT EXISTS kcal_g numeric NULL;

ALTER TABLE public.custom_foods
  ADD COLUMN IF NOT EXISTS kcal_g numeric NULL;
