---
name: project-todo-list
description: רשימת משימות פתוחות לפרויקט
metadata:
  type: project
---

## משימות פתוחות

### 🔴 דחוף
- ~~**תיקון prompt הצ'אט AI**~~ — ✅ תוקן (ai.js, בלוק ה"תזונה היום"): נוספה הוראה מפורשת ל-AI להשתמש תמיד בערך ה"סה\"כ" המוכן והמחושב מראש בקוד, ולא לחשב בעצמו מפריטי יומן המאכלים הגולמי (שם היה נוטה לטעות). **ממתין לבדיקת המשתמש בפועל בצ'אט** כדי לוודא שהתשובות נכונות עכשיו.

### 🏋️ תרגילים
- **עדכון שמות תרגילים** לפי המינוח שנלמד בקורס מדריכי כושר (מיפוי שם ישן → שם חדש, טרם סופק)
- **הרחבת מאגר התרגילים** — תרגילים נוספים

### 🍎 תזונה — שינויים בהשראת Cronometer (בעקבות מחקר והשוואה)

מוסכם, לביצוע עכשיו — **מעבר ממערכת "מנות" לגרמים חופשיים** (כרגע האפליקציה עובדת עם מנות קבועות מראש, עוברים להזנה בגרמים חופשיים כמו ב-Cronometer; המטרה חינוכית — להרגיל מתאמנים לקרוא תוויות מזון ולהבין אינטואיטיבית כמה גרם חלבון/פחמימה/שומן יש במה שהם אוכלים). **גילוי חשוב:** דיוק הגרמים כבר קיים בפועל בסורק ה-AI/USDA, אבל נזרק ב-`updateScannedTotals` (app-nutrition.js) שמעגל אותו למנות קבועות. אז המשימה היא בעיקר "להפסיק להשמיד דיוק קיים" ולא לבנות מאפס.

**תוכנית מוסכמת ל-5 שלבים, מבוצע צעד-צעד (לא הכל בבת אחת), עם 3 אופציות עיצוב לאישור בכל שלב שמשנה מסך למשתמש:**
- ✅ **שלב 1 — DB**: הוספת עמודות גרמים (`protein_g`/`carbs_g`/`fat_g` ל-daily_nutrition, `grams`/`protein_g`/`carbs_g`/`fat_g` ל-food_log) — **בוצע** (`grams_migration.sql`, הורץ בהצלחה ע"י המשתמש בסופרבייס). עמודות המנות הישנות נשארות בינתיים.
- ✅ **שלב 2 — יעדים**: `calcPortionTargets()` (app-core.js) מחזיר כעת גם `proteinGrams`/`carbsGrams`/`fatGrams` מדויקים, חשוף ב-`window._getGramTargets()`. **בוצע**
- ✅ **שלב 3 — קליטת מזון**: הסורק/הזנת הטקסט לא ממירים יותר למנות — שומרים ומציגים גרמים וקלוריות מדויקים בכל מקום (יומן מזון, עריכת פריט, מחיקה). **בוצע**
- ✅ **שלב 4 — ממשק**: כפתורי +/- הישנים הוסרו. מסך יומי חדש: קלוריות בראש + 3 שורות מאקרו (גרם + פס התקדמות ליעד), בסגנון D שאושר. הוחלף גם "Quick Add/מועדפים" ברעיון פשוט יותר שהמשתמש העדיף: **חיפוש חי** במאגר ה-USDA מתוך אותו חלון הוספת אוכל (עם צילום/גלריה/חיפוש/ברקוד-בקרוב). **בוצע**
- ✅ **שלב 5 — AI ותצוגה**: פרומפט ai.js עובד בגרמים ישירות (הוסרה הוראת ההמרה הקבועה — אין יותר צורך בה, המאקרו מדויק מגיע כמו שהוא). סטריק התזונה (checkNutritionStreak) משווה גרמים ליעד גרמים. **בוצע**

**גם עודכנו כחלק מהמכה הזו (לא היו בתוכנית המקורית אך נדרשו כדי שלא יישבר כלום):** admin.js ו-`api/save-weekly-scores.js` (ה-cron השבועי) עברו לחשב יעדים בגרמים ישירות; supabase-db.js/sw.js (סנכרון ברקע) עברו לכתוב לעמודות ה-DB הנכונות; app-report.js (תמונת הדוח המיוצאת) — התווית "ממוצע מנות ליום" הוחלפה ב"ממוצע גרם ליום"; tour.js — שלב שהצביע על כפתור שנמחק תוקן להצביע על הכפתור החדש. **חשוב: גיליתי בדרך ש-`index.html` הוא קובץ נבנה (build) מתוך `partials/*.html` דרך `build-index.js` — כל השינויים הוחלו במקור הנכון (partials) ואז index.html נבנה מחדש.**

**החלטה על נתונים היסטוריים:** לא מומרים רטרואקטיבית לגרמים (זה יהיה דיוק מזויף) — נשארים כמנות, מסמנים "מכאן ואילך מעקב בגרמים"

**⚠️ טרם נבדק בדפדפן בפועל — לבדוק:** פתיחת חלון "הוסף אוכל", חיפוש מאכל וקבלת תוצאות חיות, הוספה ליומן ועדכון הכרטיסים (קלוריות + 3 מאקרו), מחיקה/עריכה של פריט ביומן, וכן שהסטריק עדיין מתעדך נכון.

- **סריקת ברקוד** למוצרים ארוזים — פיצ'ר חדש, טרם נבנה (רק placeholder "בקרוב" בממשק)
- **"המזונות שלי" — מזון ומתכונים מותאמים אישית (פרטי לכל משתמש)** — עדיין לא נבנה, נדחה בכוונה מהמכה הזו (הוחלט בנפרד) — הבא בתור
- **גודל מנה גמיש** (יחידות מידה: גרם/כוס/יחידה/כף עם המרה) — קיים כבר בהזנת טקסט (בחירת יחידה), לא נדרש שינוי נוסף

לתחזוקה/וידוא (הפיצ'ר קיים בקוד, לא לבנות מאפס — לוודא שעובד נכון):
- ~~**דיילי סטריק (תזונה)**~~ — ✅ נמצאו ותוקנו 2 באגים ב-app-journal.js: (1) הרצף לא התאפס כשמשתמש מפספס יום — היה רק מספר שעולה בלי איפוס, תוקן עם בדיקת פער ימים (`daysDiff > 1`) שמאפסת ל-0 ומסנכרנת לשרת. (2) `checkNutritionStreak` יכל לסמן "הושג" גם ביעד 0/לא-חושב עדיין (0>=0), נוסף גיין `targetsValid` שמונע זאת. **ממתין לבדיקת המשתמש בפועל.**
- **דיילי סטריק (אימונים)** — נבדק, תקין: מחושב חי מהשרת מהיסטוריית אימונים בפועל, לא מספר שנשמר סטטי, אז מתעדכן נכון מעצמו
- **מחשבון קלורי יומי (TDEE/BMR)** — נבדק, תקין: נוסחת Mifflin-St Jeor נכונה, זהה בשלושת המקומות שהיא מופיעה בקוד (ai.js, app-core.js, app-workouts.js). הערה לעתיד (לא דחוף): כפולה 3 פעמים בקוד, כדאי לאחד למקום אחד כדי למנוע חוסר התאמה עתידי

בבדיקה, לא סגור:
- **מאגר מזון קהילתי** (מזונות שמשתמשים הכניסו, נגישים לכולם) — רעיון שנשקל, לא הוחלט. סיכון עיקרי: איכות נתונים בלי moderation (בעיה ידועה גם ב-MyFitnessPal). אופציות שעלו: (1) פרטי בלבד לכל משתמש — מה שעושים עכשיו, (2) שיתוף במעגל מצומצם/מאמן, (3) מאגר גלובלי עם דירוג/דיווח על טעויות. המלצה: לדחות החלטה לשלב שיש מסה קריטית של משתמשים

מוסכם, לביצוע:
- ~~**Quick Add / מועדפים**~~ — הוחלף בהחלטה מאוחרת יותר: חיפוש חי במאגר המזון (ראו שלב 4 למעלה) נותן גישה מהירה בלי צורך במסך "מועדפים" נפרד לניהול
- **גרפים/מגמות תזונה לאורך זמן** (לא רק יום בודד) — לבדוק אם צריך להרחיב את הדוחות הקיימים באפליקציה

לבדיקה, לא סגור:
- **חלוקה לארוחות** (בוקר/צהריים/ערב/נשנוש) ביומן המזון — מעניין, טרם הוחלט

לא רלוונטי לחזון (הוחלט לא לעשות):
- מעקב מיקרו-נוטריאנטים (84 חומרים כמו ב-Cronometer) — יותר מדי רחב/מורכב, לא פארטו
- סנכרון עם שעונים חכמים (Apple Health / Garmin / Fitbit וכו')
- קהל היעד של Cronometer (ביוהאקרים/דיאטנים) אינו קהל היעד שלנו (מתחילים)
